# План проекта: SouCampus builds — портфолио + магазин Minecraft + обучение разработке

## Бренд

Название сайта: **SouCampus builds**.

## Дизайн-система

Всё, что касается цветов, типографики, переиспользуемых UI-компонентов (`Button`, `ArrowCircle`), вёрстки и анимации перехода — вынесено в отдельный файл **[`DESIGN.md`](DESIGN.md)**. Перед любой правкой стиля/вёрстки — сверяться с ним, а не изобретать заново. Обновлять `DESIGN.md`, а не этот файл, когда меняется именно дизайн-система (новый цвет, новое правило вёрстки, новый переиспользуемый компонент).

## Статус / прогресс (обновлено 2026-07-12)

### Сделано

- Next.js 16 (App Router) + TypeScript + Tailwind v4 проект развёрнут в корне репозитория, локальный git создан, есть коммиты (автор — только пользователь, без соавторства ИИ).
- Страницы: `/` (лендинг), `/portfolio` (каталог), `/portfolio/[slug]` (динамическая деталка проекта), `/about`, `/contact`, `/shop` (заглушка).
- Компоненты лендинга (в порядке появления на главной): `Hero`, `Stats` (анимированные счётчики + цветной дивайдер на всю ширину браузера), `RecentProjects`, `CountriesMarquee` (лента флагов-кружков, управляется скроллом страницы — физически "катятся" как колёса), `ClientReviews` (карусель отзывов с спрингом-анимацией), `HowItWorks` ("как проходит заказ", 5 шагов), `PricingSection` (bento-сетка из 5 карточек тарифов), `ContactFooter`. Общие для всех страниц: `Navbar`, `PageTransition` (см. `DESIGN.md`), `Skeleton`.
- В `src/lib/projects.ts` — 4 реальные работы с настоящими фото (`public/portfolio/`): BlueSpawn, Pirate RPG Map, Sky Cathedral, Dragon Shrine.
- Весь видимый на сайте текст — на английском (комментарии в коде и `.md`-файлы остаются на русском — это внутренняя документация, не то, что видят посетители).
- `ClientReviews` — данные (`name/role/flag/text/accent`) специально оформлены как плоский массив, повторяющий будущую структуру таблицы `reviews` в БД. При подключении Supabase (Этап 3) — заменить массив на fetch из таблицы, саму карточку не менять.
- `PricingSection` — UI полностью готов (bento-сетка, стрелки, копирайтинг), тарифы/цены — черновик-плейсхолдер. Идея подписки — см. раздел "Идея на будущее: подписка на постройки" ниже.
- `PricingSection` и `ContactFooter` подключены в `src/app/layout.tsx`, а не в `page.tsx` — показываются на **каждой** странице сайта, не только на главной.
- Флаги в `CountriesMarquee` — хотлинк на `wise.com` (см. `DESIGN.md` → Изображения), это временное решение, требует замены на локальные файлы перед продакшеном.

### Осталось как плейсхолдер (заменить реальным)

- Discord-инвайт в `src/app/contact/page.tsx` — сейчас `#`.
- Текст About me — лорем ипсум, фото — `Skeleton`-заглушка.
- Отзывы клиентов в `ClientReviews.tsx` — вымышленные имена/цитаты/флаги.
- Цифры в `Stats.tsx` — placeholder-числа.
- Ответы в FAQ на `/contact` — вопросы придуманы, ответы нужно написать реальные.
- Тарифы в `PricingSection.tsx` — названия/цены/фичи черновик, нужно утвердить перед запуском.
- Флаги-картинки в `CountriesMarquee` — хотлинк на чужой CDN, заменить на свои файлы в `public/`.

## Мой уровень (для калибровки объяснений)

- Знаю HTML/CSS, немного JS.
- Синтаксис слабый: не напишу класс на JS или Python с нуля.
- Хорошо понимаю архитектурные концепции: наследование, полиморфизм, ООП в целом — теорию знаю.
- Есть опыт с FlutterFlow (low-code) и немного работы с API.
- Не уверен, что могу писать код "с нуля" самостоятельно — нужна помощь на уровне синтаксиса, а не только архитектуры.

### Как объяснять мне код

- Не считать, что я знаю синтаксис JS/TS/React — показывать конкретный код, а не только "напиши функцию, которая...".
- Архитектурные термины (компонент, состояние, пропсы, наследование и т.д.) можно использовать смело — это я пойму быстро через аналогии с ООП.
- На первых порах — больше готового кода с объяснением, что каждая часть делает, потом постепенно давать писать самому.
- Не бояться повторять базовые вещи синтаксиса JS/TS несколько раз, пока не закрепится.

## Цель проекта

Создать современный сайт-портфолио, который впоследствии вырастет в полноценный продукт:

- портфолио с работами и описаниями;
- отдельные страницы для "кто я" и контактов;
- магазин цифровых товаров, например Minecraft-карт;
- панель администратора для добавления товаров и управления контентом;
- опыт работы с современными инструментами: Next.js, TypeScript, Tailwind, Supabase, Vercel, CI/CD, Docker, а позже — более сложной архитектурой.

## Главный приоритет сейчас

1. Сначала сделать рабочий сайт-портфолио.
2. Затем добавить раздел с портфолио и отдельные страницы.
3. Потом уже делать магазин, авторизацию, сессии, оплату и админку.

## Принцип работы

- Не пытаться сделать всё сразу.
- Делать проект поэтапно, чтобы он всегда был рабочим и его можно было показать.
- Сначала простая рабочая версия, потом улучшения.
- Учиться на реальном проекте, а не только на теориях.
- Если что-то сложное — разбивать на маленькие шаги.

## Рекомендуемый стек на старте

### Frontend

- Next.js
- TypeScript
- Tailwind CSS

### Backend / данные

- Supabase как простой и быстрый вариант на старте
- PostgreSQL как более зрелый вариант в будущем

### Авторизация / пользователи

- Решено: **Supabase Auth** (не Auth.js) — раз БД уже на Supabase, это одна система вместо двух, меньше ручной настройки JWT/сессий.
- Интеграция через Supabase JS SDK прямо в Next.js (Server Actions / API routes) — отдельный backend на Python не нужен, это лишняя сложность без явной причины.
- Не делать собственную систему auth с нуля, потому что в ней много тонкостей: хеширование паролей, сессии, refresh-токены, восстановление доступа, безопасность.

### Оплата

- Stripe

### Деплой

- Vercel — это удобная PaaS-платформа для деплоя Next.js и других веб-приложений: подключаешь GitHub, пушишь код, и платформа сама собирает и разворачивает проект
- Vercel не равен VPS и не заменяет его полностью, но отлично подходит для старта, preview, простых сайтов и API
- Hostinger VPS можно рассматривать позже, когда появится необходимость в собственном сервере, Docker, более сложной инфраструктуре или когда нужно больше контроля

## Почему такой стек

- Next.js + TypeScript + Tailwind — хороший старт для современного сайта.
- Supabase проще, чем развертывать собственный backend с нуля.
- Готовые решения по авторизации и данным ускоряют разработку и снижают риск ошибок.
- Vercel удобен для демонстрации, быстрого деплоя и обучения современному workflow.
- В дальнейшем проект можно масштабировать и добавлять более сложные элементы: Docker, CI/CD, облако, API, админку, аналитики.

## План развития проекта

### Этап 1. Базовый сайт-портфолио (текущий фокус)

Цели:

- лендинг с приветствием и основной информацией;
- раздел с портфолио;
- страница "кто я";
- страница контактов;
- адаптивная верстка;
- деплой на Vercel.

Готово, когда:

- сайт открывается по публичной ссылке на Vercel;
- все страницы работают и адаптивны на мобильном;
- код лежит в Git-репозитории.

### Этап 2. Улучшение контента и структуры

Отдельные страницы проектов, лучше UI/UX, SEO и метаданные.

### Этап 3. Простая админка для контента

Мини-CMS для сайта-портфолио, что-то вроде упрощённой Тильды под себя: добавление/редактирование проектов и секций сайта без правки кода, хранение контента в Supabase. Делается до магазина, чтобы контентом можно было управлять уже на этапе портфолио.

### Этап 4. Магазин

Каталог, корзина, авторизация (Supabase Auth или Auth.js — выбрать при подходе к этапу), оформление заказа, Stripe.

### Этап 5. Продвинутая инженерия

Docker, CI/CD, тесты, логирование, безопасность, аналитика.

Этапы 2-5 — намеренно кратко: детализировать каждый непосредственно перед стартом, а не заранее.

## Архитектурный подход

На старте лучше использовать простую и понятную архитектуру:

- Frontend: Next.js App Router
- Content: локальные данные или Supabase
- Admin: простая панель для управления контентом
- Backend: минимальный, по мере необходимости

Это даст хороший баланс между:

- простотой;
- возможностью показать рабочий результат;
- обучением архитектуре;
- постепенным ростом проекта.

## Как подходить к обучению

### Что изучать параллельно

- Next.js и App Router
- TypeScript basics
- Tailwind CSS basics
- Git и GitHub
- базовые принципы UI/UX
- деплой через Vercel
- работу с переменными окружения и секретами
- базовые принципы безопасности

### Как работать с ИИ

- разбивать большие задачи на маленькие;
- задавать конкретные вопросы;
- не бояться переспросить;
- просить объяснение архитектуры и причин выбора решений;
- проверять результат, а не blindly копировать код.

## Правила для проекта

- Использовать простые решения там, где это уместно.
- Не усложнять архитектуру без причины.
- Сначала сделать рабочую версию, потом улучшать.
- Хранить секреты в переменных окружения.
- Не публиковать токены и ключи в репозитории.
- Делать изменения постепенно и документировать их.
- Git-репозиторий уже создан локально (Этап 1); автор коммитов — только пользователь, без соавторства ИИ в коммитах.
- Приоритет производительности: сайт должен грузиться максимально быстро (оптимизация картинок, минимум лишнего JS на клиенте — см. Server/Client Components).
- Для медленных/асинхронных блоков (загрузка данных, изображений) — использовать skeleton-плейсхолдеры вместо пустого экрана или спиннеров.

## Юридическое

- Нужно будет написать Terms of Service (условия предоставления услуг фриланс-заказов на Minecraft-карты) — отложено, сделать ближе к запуску магазина/приёму заказов (Этап 4).

## Что будет делать этот проект для обучения

Этот проект должен стать практической площадкой для:

- изучения современного веб-разработки;
- понимания архитектуры приложений;
- работы с базами данных и авторизацией;
- деплоя и DevOps-основ;
- использования AI-агентов в реальном процессе разработки.

## Структура сайта-портфолио

Контекст: пользователь — фрилансер, строит карты на заказ в Minecraft, есть много готовых работ (картинки будут, описания пока placeholder). Отдельно в будущем планируется "портфолио разработчика" с пет-проектами (например, автоматизации на n8n) — не сейчас, отдельная задача.

### Навигация (навбар)

Home · Portfolio · Shop · About me · Contact

- Shop — в будущем отдельный поддомен (например `shop.domain.com`), не часть основного Next.js роутинга с самого начала.

### Главная страница (Home)

Актуальный порядок секций и что в них — см. "Сделано" выше (список компонентов лендинга).

### Остальные страницы

- **Portfolio** — полный каталог работ (карты) в виде мини-карточек (превью + краткое описание). Клик по карточке открывает **отдельную подстраницу** конкретной работы: `src/app/portfolio/[slug]/page.tsx` (Next.js dynamic route) — там расширенное описание, сроки/дедлайны выполнения, галерея фотографий.
- **About me** — история, опыт, чем занимаешься.
- **Contact** — контакт через Discord (ссылка-приглашение на сервер, сейчас плейсхолдер `#` в `src/app/contact/page.tsx`, нужно заменить на реальный инвайт).
- **Shop** (позже, поддомен) — магазин цифровых товаров.

## Структура магазина (на уровне идеи, детали — при подходе к Этапу 4)

- Каталог товаров (карты/цифровые товары).
- Карточка товара с описанием и превью.
- Корзина.
- Оформление заказа + Stripe.
- Отдельный поддомен, авторизация через Supabase Auth.

## Идея на будущее (отдельный проект, не часть текущего сайта)

Настраиваемый Discord-бот для тикетов (для серверов, в т.ч. для приёма заказов на этом сервере). Отдельная задача вне текущего плана сайта — вернуться к ней после Этапа 1-3.

## Идея на будущее: подписка на постройки (Patreon-модель внутри магазина)

Помимо разовых заказов карт на заказ, в магазине (Этап 4) рассмотреть модель подписки в духе Patreon:

- несколько платных тарифов (черновик — см. `PricingSection.tsx` на главной, UI уже сделан как драфт);
- подписчики получают доступ к библиотеке готовых построек, которые автор выкладывает регулярно;
- полученные по подписке постройки закрепляются в личном профиле пользователя на сайте (то есть нужен профиль/аккаунт пользователя — завязано на Supabase Auth, Этап 4);
- более высокие тарифы — дополнительные права (например, коммерческая лицензия на использование построек).

Статус: пока только идея и готовый UI-черновик секции на главной странице. Логика подписок, оплата, привязка построек к профилю — не реализованы, планировать при подходе к Этапу 4 (магазин + авторизация).

## Следующий шаг

Текущая фаза: **проход по контенту и дизайну одновременно** — пользователь активно правит дизайн-детали (цвета, шрифты, анимации) итеративно, помимо этого остаётся открытым вопрос реального контента (см. плейсхолдеры выше).

1. Заполнить контент: реальный Discord-инвайт, текст About me, реальные отзывы клиентов, реальные цифры в Stats, реальные тарифы в PricingSection.
2. Заменить хотлинк флагов в `CountriesMarquee` на локальные файлы.
3. Деплой на Vercel (ещё не делали).
4. Дальше по плану — Этап 3 (мини-админка на Supabase). Перед проектированием схемы Supabase/админки имеет смысл переключиться на более сильную модель — для верстки и контента разницы нет.

Если потребуется, этот план можно будет менять по ходу работы.
